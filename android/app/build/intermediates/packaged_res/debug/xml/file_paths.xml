<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Downloads folder for generated PDFs -->
    <external-path name="external_downloads"
        path="Download/" />
    <!-- App external files dir (fallback) -->
    <external-files-path name="external_files"
        path="." />
    <!-- Internal cache dir (fallback) -->
    <cache-path name="cache"
        path="." />
</paths>
